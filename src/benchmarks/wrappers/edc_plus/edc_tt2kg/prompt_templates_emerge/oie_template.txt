Your task is to update Wikidata knowledge graph from the knowledge explicitly or implicitly contained in a given text. Two sets of triples are to be extracted: 'Triplets in text', which contain triples relating entities mentioned in text in the form of [Entity1, Relationship, Entity2, Action], where action indicates if the triple has to be added (action 'ADD') or deprecated (action 'DEPRECATE') from the graph based on the knowledge in text. The second set of triples is called 'Triples not in text', and consists of triples with one entity (head or tail) mentioned in text and the other entity not mentioned in text but potentially existing in the graph.

In your answer, please strictly only include the triples and do not include any explanation or apologies; also DO NOT repeat same triple multiple times.

Here are some examples:
{few_shot_examples}

Extract at least one triple from the text. Do not start with an empty list. Begin after 'Triples in text:' and also generate 'Triples not in text' as in the examples above.
Text: {input_text}